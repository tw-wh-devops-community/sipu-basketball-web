import React from 'react';
import PickIcon from '../../../assets/PickIcon';

const Header: React.FC = () => (
  <div className="header">
    <PickIcon />
    <h4 className="title">选择场地</h4>
  </div>
);

export default Header;
